# Backend API (subscription plans, FAQs, legal pages)
# Server-side calls use this URL directly; browser calls go through /api/proxy (no CORS).
API_URL=https://api.neyome.com
NEXT_PUBLIC_API_URL=https://api.neyome.com
